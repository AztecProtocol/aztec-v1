const isOriginBelongsToApiKey = require('./isOriginBelongsToApiKey');
const balance = require('./balance');
const getDappInfo = require('./getDappInfo');

module.exports = {
    balance,
    isOriginBelongsToApiKey,
    getDappInfo,
};
