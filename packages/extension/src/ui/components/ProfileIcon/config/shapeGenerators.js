import userIconShape from '../utils/userIconShape';
import noteIconShape from '../utils/noteIconShape';

export default {
    user: userIconShape,
    asset: noteIconShape,
    token: noteIconShape,
};
