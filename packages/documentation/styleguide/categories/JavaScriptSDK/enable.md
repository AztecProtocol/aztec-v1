This is how you enable AZTEC

<!-- ```jsx
import apiKey from '../../config/apiKey';
import loadSDK from '../../utils/loadSDK';

async function enable() {
    await loadSDK();
    const result = await window.aztec.enable({
        apiKey,
    });
    return result
};

<Demo demoScript={enable} text="Enable SDK" />;
``` -->
