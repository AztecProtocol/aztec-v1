{
  "name": "aztec-monorepo",
  "private": true,
  "engines": {
    "node": ">=4"
  },
  "workspaces": [
    "packages/**"
  ],
  "devDependencies": {
    "lerna": "^3.10.6"
  },
  "scripts": {
    "bootstrap": "lerna link && lerna exec npm install"
  }
}
