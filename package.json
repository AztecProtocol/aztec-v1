{
  "name": "aztec-monorepo",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.1"
  },
  "scripts": {
    "bootstrap": "lerna exec -- npm install && lerna run compile"
  },
  "workspaces": [
    "packages/*"
  ]
}
